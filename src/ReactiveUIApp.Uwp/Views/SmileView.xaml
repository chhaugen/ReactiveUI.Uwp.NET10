<local:ReactiveSmileView
    x:Class="ReactiveUIApp.Uwp.Views.SmileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ReactiveUIApp.Uwp.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:ReactiveUIApp.Core.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:SmileViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="1" Grid.Column="1">
            <!--  Description: Emoji2  -->
            <FontIcon
                FontFamily="Segoe MDL2 Assets"
                FontSize="300"
                Glyph="&#xE76E;" />
        </Grid>
    </Grid>
</local:ReactiveSmileView>
